spring:
  config:
    activate:
      on-profile: test

  datasource:
    url:
    username:
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

logging:
  level:
    root:
    com.joshua.expenseapi:

# Em testes, normalmente vamos querer algo mais descartável e menos ruído.